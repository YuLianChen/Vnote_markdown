如果出现无法连接，不出现确定按钮
```shell
#两个排查命令
sudo systemctl status gnome-remote-desktop.service --no-pager
sudo journalctl -u gnome-remote-desktop.service -b -l --no-pager | tail -n 80
#临时试一次,登录进去
sudo setenforce 0
sudo systemctl restart gnome-remote-desktop.service
```