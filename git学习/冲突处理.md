# 远端拉取冲突处理
如果和远端仓库出现冲突，则
```c
git stash        # 将本地修改暂存起来
git pull         # 成功拉取远程更新（Fast-forward）
git stash pop    # 将暂存的修改恢复回来
```
`git stash pop` 合并代码的时候，并不是将我的修改直接覆盖到远端的更新上，而是智能根据上下文，将代码合并到一起，如果有冲突则手动处理冲突